
version: 0.0
os: linux
files:
  - source: /
    destination: /root/aws-code-deploy/rxwala-api/aws_code_deploy_tmp
file_exists_behavior: OVERWRITE
permissions:
  - object: /
    pattern: "**"
    owner: root
    group: root
hooks:
  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 600
      runas: root
  # AfterInstall:
  #   - location: scripts/after_install.sh
  #     timeout: 300
  #     runas: root
  AfterInstall: 
    - location: scripts/after_install.sh
      timeout: 600
      runas: root
  ApplicationStart:
    - location: scripts/start_server.sh
      timeout: 300
      runas: root
  # ApplicationStop:
  #   - location: scripts/stop_server.sh
  #     timeout: 300
  #     runas: root

